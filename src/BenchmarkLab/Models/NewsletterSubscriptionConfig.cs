namespace MeasureThat.Net.Models
{
    public class NewsletterSubscriptionConfig
    {
        public readonly bool Enabled;

        public NewsletterSubscriptionConfig(bool enabled)
        {
            Enabled = enabled;
        }
    }
}
